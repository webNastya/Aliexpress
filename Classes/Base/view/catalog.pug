each card in cards
    .card-wrapper
        .card-img-wrapper
            img.card-img-top(data-each-id=card.id src="src/img/carts/"+card.id+"/0.png" alt='')
            button.add-favorite(class=card.inFavorites ? "active" : "" data-id=card.id)
        .card-body.justify-content-between
            .card-price !{card.price.toString().replace(/(\d)(?=(\d{3})+(\D|$))/g, '$1 ')} ₽
            .card-title(data-each-id=card.id) !{card.title}
            - let attributes = card.inBasket ? {disabled: "true"} : {};

.each-good-content
    p
        a(href='#') Категория
        |  > Какой-то товар
    .card-content
        img.each-good-img(src=card.img alt='')
        .each-good-description
            h1.each-good-title Main title
            p.each-good-title !{card.title}
            p.each-good-price
                | !{card.price.toFixed(2).toString().replace(/(\d)(?=(\d{3})+(\D|$))/g, '$1 ')} ₽
                button.like-each-card(class=card.inFavorites ? "active" : "" data-id=card.id)
            .calc
                - let attributes = card.inBasket ? {disabled: "true"} : {};
                button.add-card(data-id=card.id)&attributes(attributes) Добавить товар
